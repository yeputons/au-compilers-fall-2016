fun foo(n) begin
  if n == 0 then
    return 1
  fi;
  n := n - 1;
  res := foo(n) + foo(n) + foo(n) + foo(n) +
         foo(n) + foo(n) + foo(n) + foo(n);
  return res
end

x := read();
write(foo(x))
